<ul>
  <% @person.phone_numbers.each do |phone|  %>
    <li><%= phone.number  %><%= link_to('edit', edit_phone_number_path(phone))  %></li>
  <% end  %>
  <li><%= link_to 'Add phone number', new_phone_number_path(person_id: @person.id) %></li>
</ul>